# Retrom

Centralized game library / collection management with a focus on emulation (image `ghcr.io/jmberesford/retrom-service:v0.8.4`).

The web UI and desktop API are both on port 5101 (`/web`). Mount a library at `/app/library`. Primary targets: k3s + Rancher.
