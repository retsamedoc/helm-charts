# Minimal overrides for chart-testing install
controllers:
  main:
    containers:
      main:
        env:
          TZ: UTC
          MQTT_HOST: mqtt.example.local
service:
  main:
    controller: main
