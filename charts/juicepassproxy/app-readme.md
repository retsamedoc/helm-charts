# JuicePassProxy

Proxy UDP traffic between Juicebox EV chargers and MQTT for Home Assistant discovery.

Uses this repository’s common library (`retsamedoc.common.*`, `controllers` / `service` values). Primary targets: k3s + Rancher. UDP / hostNetwork considerations may apply on LAN installs — see chart README.
