# ESPHome

ESPHome dashboard for managing ESP8266/ESP32 devices via configuration files and Home Assistant.

Configure via `values.yaml` (image, env, service, ingress, persistence). Primary targets: k3s + Rancher.
