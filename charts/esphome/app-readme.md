# ESPHome

ESPHome dashboard for managing ESP8266/ESP32 devices via configuration files and Home Assistant.

Dashboard on port 6052. Configure via `values.yaml` (image, env, service, ingress, persistence at `/config`). Primary targets: k3s + Rancher.
