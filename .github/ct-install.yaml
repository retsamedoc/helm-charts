remote: origin
target-branch: main
helm-extra-args: --timeout 600s
chart-dirs:
  - charts
# Install only charts that are smoke-testable in k3d.
# Application charts on in-repo common (file://../common) are install-tested.
# common is a library chart and is not installed standalone.
excluded-charts:
  - common
chart-repos:
  - bitnami=https://charts.bitnami.com/bitnami
  - library=https://bjw-s-labs.github.io/helm-charts
  - retsamedoc=https://retsamedoc.github.io/helm-charts
  - jetstack=https://charts.jetstack.io
