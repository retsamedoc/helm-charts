remote: origin
target-branch: main
helm-extra-args: --timeout 600s
chart-dirs:
  - charts
# Install only charts that are smoke-testable in k3d.
# juicepassproxy is the first app on in-repo common (file://../common).
# weatherflow2mqtt remains excluded until its values use the controllers API.
# esphome/retrom/romm stay excluded until migrated off bjw-s common and install
# CI values are hardened for real app startup.
excluded-charts:
  - common
  - esphome
  - retrom
  - romm
  - weatherflow2mqtt
chart-repos:
  - bitnami=https://charts.bitnami.com/bitnami
  - library=https://bjw-s-labs.github.io/helm-charts
  - retsamedoc=https://retsamedoc.github.io/helm-charts
  - jetstack=https://charts.jetstack.io
